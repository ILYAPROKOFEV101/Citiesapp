<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/bottomSheetContainer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:fillViewport="true"
    android:background="@android:color/white"
    android:clipChildren="false"
    android:clipToPadding="false">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:clipChildren="false"
        android:clipToPadding="false">

        <!-- Кнопка закрытия/открытия BottomSheet -->
        <ImageButton
            android:id="@+id/expandIndicator"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Toggle bottom sheet"
            android:src="@drawable/stat_minus_2_24px" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/carouselRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="232dp"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:orientation="horizontal"
            android:paddingTop="100dp" />

        <TextView
            android:id="@+id/fullListName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Список городов"
            android:textSize="18sp"
            android:gravity="center"
            android:paddingTop="8dp" />

        <!-- Здесь можешь добавить список городов выбранного набора -->
    </LinearLayout>
</androidx.core.widget.NestedScrollView>
